{
  "name": "figma-shape-to-frame",
  "module": "code.ts",
  "type": "module",
  "scripts": {
    "build": "bun build ./code.ts --outdir . --target browser",
    "watch": "bun build ./code.ts --outdir . --target browser --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.107.0",
    "bun-types": "latest"
  }
}
